# SPDX-FileCopyrightText: 2026 Shun Sakai
#
# SPDX-License-Identifier: CC0-1.0

name: rumdl

on:
  pull_request:
  push:
    branches:
      - "develop"
      - "main"
  workflow_dispatch:

jobs:
  rumdl:
    name: rumdl
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Run rumdl
        uses: rvben/rumdl@v0.1.36
